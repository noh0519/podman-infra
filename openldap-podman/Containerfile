FROM docker.io/library/debian:bookworm-slim

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
    && echo "slapd slapd/no_configuration boolean true" | debconf-set-selections \
    && apt-get install -y --no-install-recommends slapd ldap-utils openssl ca-certificates \
    && rm -rf /var/lib/apt/lists/* \
    && rm -rf /var/lib/ldap/*

COPY scripts/entrypoint.sh scripts/generate-tls.sh /usr/local/bin/
RUN chmod 0755 /usr/local/bin/entrypoint.sh /usr/local/bin/generate-tls.sh

EXPOSE 389 636
VOLUME ["/var/lib/ldap", "/etc/ldap/slapd.d", "/etc/ldap/tls"]

ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]
